module.exports={
    base: '/leetcode/',
    dest: '../docs',
    plugins: ['autobar'],
    themeConfig: {
        nav: [
            { text: 'Home', link: '/' }
          ],
        displayAllHeaders: false // 默认值：false
      }
}